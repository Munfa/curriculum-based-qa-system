# --- Large generated data (do NOT commit; share via cloud storage) ---
# The vector index and cleaned corpus are large binary/data artifacts.
# They are regenerated by clean_corpus.py and build_index.py, and shared
# with teammates via Drive/Dropbox rather than git.
index_v1/
index/
cleaned/

# --- Raw dataset (not redistributable; each member downloads from NCTB/Mendeley) ---
NCTB-SchoolText*/
*.zip

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# --- Model / HF cache (can be many GB) ---
.cache/
*.safetensors
*.bin

# --- OS / editor cruft ---
.DS_Store
.vscode/
.idea/
*.log